# Gatherer's clearing — illustration candidate

Created 2026-09-24 using built-in image generation. Not approved.

Selected generated output:
`exec-abf990b8-45f7-4c5e-ac08-7cb1227c342e.png`, copied unchanged to
`illustration.png`. Earlier full-scene-reference draft was NOT selected.

Prompt: `ILLUSTRATION_PROMPT.txt`.

Input roles:

1. `environment.png`: Blender camera and geometry only.
2. `../character-only-references/mushroom.png`: approved character model only.
3. `../character-only-references/merchant.png`: approved character model only.

The last two are native source-renderer exports on transparent canvases, with
no background scenes or UI. Character features are style examples, not a request
to place people in the generated plate. NPC rendering remains separate in lab.

The image follows the overall blockout but does not guarantee exact projected
corners, dimensions or perspective. Model views remain available for comparison.
Do not turn this candidate into an environment baseline without user approval.
